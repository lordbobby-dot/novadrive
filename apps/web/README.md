# NovaDrive Web

Next.js frontend. See the [root README](../../README.md) for setup and the
[roadmap](../../ROADMAP.md) for architecture/milestone context.

```bash
pnpm --filter web dev   # start in dev mode
```

Runs at http://localhost:3000. Needs the API running (`pnpm --filter api dev`) for anything
beyond static pages.
